initGame();

function initGame() {
    let test = document.querySelector('.game')
    console.log(test)
    test.insertAdjacentHTML(
        `beforebegin`,
        `<p>HellloooooooVibes</p>`
    )

}
